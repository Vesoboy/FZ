@model DataBaseSite



<form asp-action="ActiveTrue" asp-controller="home" method="post">

    <div class="form-group">
        <input asp-for="Id" class="d-none" />
    </div>

    <div class="form-group">
    <input type="text" asp-for="Url" class="d-none" />
    </div>

    <div class="form-group">
    <input type="text" asp-for="Message" class="d-none" />
    </div>

    <div class="form-group">
    <input type="checkbox" asp-for="Active" class="d-none" />
    </div>

	<div >
        <input type="submit" value="Вкл сайт" class="btn btn-outline-success" />
	</div>
    
</form>











@*<div class="btn btn-light" id="toggleActive">
    @if (Model.Active)
    {
        <span>Выключить сайт</span>
    }
    else
    {
        <span>Включить сайт</span>
    }
</div>

<form id="toggleActiveForm-@Model.Id" asp-action="ActiveTrue" asp-controller="home" method="post">
    <input type="hidden" asp-for="Url" />
    <input type="hidden" asp-for="Message" />
    <input type="hidden" asp-for="Id" />
    <input type="hidden" asp-for="Active" />
</form>

<script>
    const toggleActiveBtn = document.getElementById('toggleActive');
    const toggleActiveForm = document.getElementById('toggleActiveForm-@Model.Id');
    const activeCheckbox = toggleActiveForm.querySelector('input[name="Active"]');
    toggleActiveBtn.addEventListener('click', () => {
        if (activeCheckbox.value === 'True') {
            activeCheckbox.value = 'False';
            toggleActiveBtn.innerHTML = '<span>Включить сайт</span>';
            toggleActiveBtn.classList.remove('btn-danger');
            toggleActiveBtn.classList.add('btn-light');
        } else {
            activeCheckbox.value = 'True';
            toggleActiveBtn.innerHTML = '<span>Выключить сайт</span>';
            toggleActiveBtn.classList.remove('btn-light');
            toggleActiveBtn.classList.add('btn-danger');
        }
        toggleActiveForm.submit();
    });
</script>*@

